# راهنمای بکاپ و بازیابی

> همهٔ موارد این سند روی سیستم واقعی تست شده‌اند (ساخت اسنپ‌شات، حذف عمدی داده، بازگردانی موفق).

## ۱. فایل‌ها کجا ذخیره می‌شوند؟

همه‌چیز داخل یک پوشهٔ واحد به نام `DATA_DIR` است:

| محیط | مسیر `DATA_DIR` |
|---|---|
| Railway | `/app/data` (روی Volume) |
| VPS (طبق `install.sh`) | `/var/lib/shirini-data` |
| اجرای محلی | پوشهٔ جاری پروژه |

محتویات آن:

```
DATA_DIR/
├── data.json              ← دیتای اصلی: سفارش‌ها، مشتری‌ها، محصولات، فاکتورها
├── data.prev.json         ← آخرین نسخهٔ سالم قبلی (خودکار)
├── data.bak.1..5.json     ← ۵ نسخهٔ چرخشی ساعتی (خودکار)
├── data.corrupt.json      ← فقط اگر فایل اصلی خراب شده باشد (برای بررسی)
├── settings.json          ← تنظیمات پنل و بات (رمز و توکن اینجاست)
├── userStates.json        ← وضعیت لحظه‌ای مکالمهٔ کاربران با بات
├── userCarts.json         ← سبد خرید کاربران
├── backups/               ← اسنپ‌شات‌های کامل پنل (shirinkam-backup-*.json)
├── product-images/        ← عکس محصولات
└── telegram-file-cache/   ← کش فیش‌ها و عکس‌های تلگرام
```

**مهم:** برای بکاپ کامل باید **کل پوشهٔ `DATA_DIR`** را بردارید، نه فقط `data.json`. عکس‌های محصولات و فیش‌ها داخل `data.json` نیستند.

---

## ۲. سه لایهٔ محافظت

### لایهٔ ۱ — محافظت خودکار در برابر خرابی (همیشه روشن)

نوشتن روی `data.json` **اتمیک** است: اول فایل موقت، بعد `fsync`، بعد `rename`. یعنی اگر وسط ذخیره سرور ری‌استارت شود یا برق برود، فایل هیچ‌وقت نصفه نمی‌ماند.

اگر با این حال `data.json` خراب شود، موقع بالا آمدن **خودکار** از `data.prev.json` و بعد از `data.bak.1…5` بازیابی می‌شود و فایل خراب به‌عنوان `data.corrupt.json` نگه داشته می‌شود.

این لایه نیاز به تنظیم ندارد.

### لایهٔ ۲ — اسنپ‌شات‌های پنل (باید روشنش کنی)

در پنل → **بکاپ و بازیابی**:

- **ساخت دستی:** دکمهٔ ساخت نسخهٔ پشتیبان → فایل در `DATA_DIR/backups/` ذخیره می‌شود.
- **دانلود:** دکمهٔ خروجی/Export → یک فایل JSON روی کامپیوتر خودت.
- **بکاپ خودکار:** ⚠️ **به‌صورت پیش‌فرض خاموش است.** روشنش کن و روی `هر ۶ ساعت` بگذار. گزینهٔ «اطلاع‌رسانی به تاپیک تلگرام» را هم فعال کن تا خلاصهٔ هر بکاپ در گروهت بیاید.
- `keepLastSnapshots` (پیش‌فرض ۱۰) تعیین می‌کند چند نسخه نگه داشته شود.

**بازگردانی:** در همان صفحه، روی اسنپ‌شات مورد نظر → بازگردانی. سیستم **قبل از بازگردانی خودکار یک اسنپ‌شات ایمنی** (`pre_restore_safety`) می‌سازد، پس اگر اشتباه بازگرداندی قابل برگشت است.

> نکتهٔ امنیتی مثبت: رمز پنل و توکن بات **داخل فایل بکاپ نیستند** (تست شد). پس فایل بکاپ را می‌شود با خیال راحت‌تری جابه‌جا کرد — ولی همچنان حاوی اطلاعات مشتریان است، مراقبش باش.

### لایهٔ ۳ — بکاپ بیرون از سرور (مهم‌ترین)

بکاپی که روی همان دیسکِ سرور است، در برابر از دست رفتن خود سرور بی‌فایده است.

**روی VPS** — این را در `crontab -e` بگذار (هر شب ۳ صبح، نگهداری ۱۴ روز):

```bash
0 3 * * * tar -czf /root/shirini-$(date +\%Y\%m\%d).tar.gz /var/lib/shirini-data && find /root -name 'shirini-*.tar.gz' -mtime +14 -delete
```

بعد هفته‌ای یک‌بار یکی از این فایل‌ها را **جای دیگری** کپی کن (کامپیوتر خودت، فضای ابری، یا حتی تلگرام).

**روی Railway** — چون به فایل‌سیستم دسترسی مستقیم نداری، از دکمهٔ Export در پنل استفاده کن یا بکاپ خودکار + اطلاع‌رسانی تلگرام را روشن کن.

---

## ۳. چطور بکاپ را برگردانیم؟

### روش الف — از داخل پنل (ساده‌ترین)
بکاپ و بازیابی → انتخاب اسنپ‌شات → بازگردانی. یا اگر فایل JSON دانلودی داری، دکمهٔ Import.

### روش ب — دستی روی VPS (وقتی پنل بالا نمی‌آید)

```bash
pm2 stop shirini                                  # ۱. سرویس را بخوابان
cp -r /var/lib/shirini-data /root/before-restore  # ۲. از وضعیت فعلی کپی بگیر
tar -xzf /root/shirini-20260919.tar.gz -C /       # ۳. بکاپ را باز کن
pm2 start shirini                                 # ۴. سرویس را بالا بیاور
pm2 logs shirini --lines 50                       # ۵. لاگ را چک کن
```

### روش ج — فقط برگرداندن `data.json` خراب

```bash
pm2 stop shirini
cd /var/lib/shirini-data
cp data.prev.json data.json     # یا data.bak.1.json
pm2 start shirini
```

---

## ۴. چک‌لیست

- [ ] بکاپ خودکار پنل را روشن کن (الان خاموش است) — هر ۶ ساعت
- [ ] cron بکاپ بیرونی را روی VPS بگذار
- [ ] هر از گاهی یک بکاپ را جای دیگری کپی کن
- [ ] **یک‌بار بازیابی را تست کن** — بکاپ تست‌نشده یعنی بکاپ نداری

---

## ۵. دربارهٔ دیتابیس

الان از فایل JSON استفاده می‌شود و برای این حجم کار **کاملاً کافی است**: با ۲۰ هزار سفارش (حدود ۵ سال کار) هر ذخیره ۲۵ میلی‌ثانیه طول می‌کشد.

اگر روزی لازم شد، **SQLite** گزینهٔ درست است نه MySQL — یک فایل کنار بقیه، همین بکاپ فعلی شاملش می‌شود، و سرویس جداگانه/پورت/رمز نمی‌خواهد. MySQL فقط وقتی معنی دارد که چند سرور جدا بخواهند به یک دیتا وصل شوند.
